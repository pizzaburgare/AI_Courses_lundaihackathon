# The only variable grasp requires.
OPENROUTER_API_KEY=<YOUR_OPENROUTER_API_KEY>

# Optional: overrides DEFAULT_MODEL for every LLM call in a run.
# Equivalent to passing `grasp --model <id>` on the command line.
# GRASP_MODEL=google/gemini-3.1-pro-preview
